// import React from 'react';
// import TestRenderer from 'react-test-renderer';

// import { App } from '../App';

describe('<App />', () => {
  it('renders', () => {
    // const instance = TestRenderer.create(<App />);
    // expect(instance.root.children.length).toBe(1);
    expect(true).toBe(true);
  });
});
