export type FollowAnalyticsData = {
  includeReason?: string;
  on?: string;
  castHash?: string;
  sourceSurface?: 'home_feed';
  via?: string;
  followSuggestionReason?: string;
  interestName?: string;
};
